import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function IssuePage() {
  return (
    <PlaceholderScreen
      eyebrow="ISSUE"
      title="起票実行"
      subtitle="共有ドライブに月次フォルダを作成し、個別精算書を一括作成します。"
      description="対象・対象月の選択、保存先フォルダ名プレビュー、未提出者を除いた一括作成と実行ログ表示を実装予定。"
    />
  );
}
